export const environment = {
  production: true,
  firebaseConfig: {
    apiKey: "FROM_VAULT",
    authDomain: "FROM_VAULT",
    databaseURL: "FROM_VAULT",
    projectId: "FROM_VAULT",
    storageBucket: "FROM_VAULT",
    messagingSenderId: "FROM_VAULT",
    appId: "FROM_VAULT"
  }
};
